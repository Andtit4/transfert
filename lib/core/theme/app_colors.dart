import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  // static const primary = Color(0xFF92d336); // Orange clair pour le bouton
  static const primary = Color(0xFF008080);
  static const secondary = Color(0xFF6C757D);
  static const background = Colors.white;
  static const backgroundBlack = Colors.black;
  static const surface = Colors.white;

  // Couleurs texte
  static const textPrimary = Color(0xFF212529);
  static const textSecondary = Color(0xFF6C757D);
  static const textHint = Color(0xFFA0A5AA);

  static const textPrimaryBlack = Colors.white;
  static const textSecondaryBlack = Color.fromARGB(255, 198, 198, 199);

  // Couleurs d'étatR
  static const error = Color(0xFFDC3545);
  static const success = Color(0xFF28A745);
  static const info = Color(0xFF17A2B8);
  static const warning = Color(0xFFFFC107);

  // Couleurs des inputs
  static const inputBorder = Color(0xFFF0F9F9);
  static const inputColor = Color(0xFFF0F9F9);
  static const inputBgBlack = Color.fromARGB(255, 66, 66, 66);

  static const inputFocused = Color(0xFF80BDFF);
}
